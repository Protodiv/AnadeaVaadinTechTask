apiVersion: v1
kind: Service
metadata:
  name: auth-postgres-db
  namespace: anadea
  labels:
    app: auth-postgres-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
  selector:
    app: auth-postgres-db