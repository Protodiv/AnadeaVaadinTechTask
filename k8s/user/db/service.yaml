apiVersion: v1
kind: Service
metadata:
  name: user-postgres-db
  namespace: anadea
  labels:
    app: user-postgres-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
  selector:
    app: user-postgres-db