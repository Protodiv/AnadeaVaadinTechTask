apiVersion: v1
kind: PersistentVolume
metadata:
  name: user-postgres-db-volume
  namespace: anadea
  labels:
    type: local
    app: user-postgres-db
spec:
  storageClassName: manual
  capacity:
    storage: 250Mi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /data/user_postgres_db

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: user-postgres-db-volume-claim
  namespace: anadea
  labels:
    app: user-postgres-db
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 250Mi